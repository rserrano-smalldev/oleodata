[pytest]
asyncio_mode = auto
# _init_database (tests/conftest.py) es session-scoped: el loop tiene que
# ser igual de amplio, si no pytest-asyncio lanza ScopeMismatch.
asyncio_default_fixture_loop_scope = session
